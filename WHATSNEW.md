### Breaking Changes

- Rename `GeneralException`, `GeneralWarning` and `GeneralError` to `FotooboException`, 
  `FotooboWarning` and `FotooboError` respectively.

### Added

### Changed

- Give nicer console output when a `FotooboError` or `FotooboWarning` reaches the main() of fotoobo.

### Removed

